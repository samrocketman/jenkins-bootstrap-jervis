#!/bin/sh
#Add potentially missing user groups
getent group @@USER@@ &> /dev/null || groupadd -r @@USER@@
getent passwd @@USER@@ &> /dev/null || useradd -r -g @@USER@@ -s /sbin/nologin -d "@@HOME@@" -c "@@SUMMARY@@" @@USER@@
